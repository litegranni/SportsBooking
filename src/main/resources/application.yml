spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sports_booking
    username: root
    password: Norrmejerier4
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        id:
          new_generator_mappings: true
